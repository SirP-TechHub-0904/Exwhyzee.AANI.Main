@page
@model Exwhyzee.AANI.Web.Areas.Alumni.Pages.Dashboard.FamiliesonaaniModel

@{
    ViewData["Title"] = "Familiesonaani";
}

<h1>Familiesonaani</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParticipantFamiliesOnSEC[0].Participant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParticipantFamiliesOnSEC[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ParticipantFamiliesOnSEC[0].SubCategoryFamiliesOnSEC)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ParticipantFamiliesOnSEC) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Participant.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SubCategoryFamiliesOnSEC.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
