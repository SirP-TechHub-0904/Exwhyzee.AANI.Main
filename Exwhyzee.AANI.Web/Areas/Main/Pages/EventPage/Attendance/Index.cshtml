@page
@model Exwhyzee.AANI.Web.Areas.Main.Pages.EventPage.Attendance.IndexModel

@{
    ViewData["Title"] = "Index";
}

<div class="card">
    <div class="card-header">
        <h3 class="card-title">EVENT ATTENDANCE/h3>

        <a asp-page="./Create" class="btn bg-gradient-warning btn-sm" style="float:right;">Create New</a>
    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <div class="table-responsive">
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>S/N</th>
            <th>
                @Html.DisplayNameFor(model => model.EventAttendance[0].Participant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventAttendance[0].DatetimeArrival)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventAttendance[0].DatetimeDeparture)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventAttendance[0].Event)
            </th>
            <th></th>
        </tr>
                </thead>  @{
                    int sn = 0;
                }
    <tbody>
@foreach (var item in Model.EventAttendance) {
        <tr>
                            <td>
                                @{
                                    sn++;
                                }
                                @sn
                            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Participant.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatetimeArrival)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DatetimeDeparture)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Event.Id)
            </td>
                            <td>
                                <div class="btn-group">
                                    <button type="button" class="btn btn-success">Action</button>

                                    <div class="btn-group">
                                        <button type="button" class="btn btn-success dropdown-toggle dropdown-icon" data-toggle="dropdown" aria-expanded="false">
                                        </button>
                                        <div class="dropdown-menu" style="">

                                            <a class="dropdown-item" asp-page="./Edit" asp-route-id="@item.Id">Edit</a>

                                        </div>
                                    </div>
                                </div>

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>