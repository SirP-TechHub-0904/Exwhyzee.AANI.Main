@using Exwhyzee.AANI.Web.Helper.BaseHelper;
@using Exwhyzee.AANI.Domain.Models;
@inject IBaseModel BaseModel;
@using System.Text.RegularExpressions;
@{
    Regex regex = new Regex("[^a-zA-Z0-9]");
    int listcountforhome = 500;
    // Get the URL for the home page
    string homeUrl = Url.Page("/Index");
    // Get the current URL
    string currentUrl = Context.Request.Path;
    // Check if the current URL is the home page URL
    bool isHomePage = currentUrl.Equals(homeUrl, StringComparison.OrdinalIgnoreCase);

    string position = Model.position;
    string pageset = Model.pageset;
    long? id = Model.id;
    long? secid = Model.secid;

    List<PageSection> PageSectionList = await BaseModel.GetPageSections(position, pageset, id, secid);
}
@foreach (var item in PageSectionList)
{
    if (item.TemplateKey == "1")
    {
        <!--About One Start -->
        <section class="about-one @item.CustomClass">

            <div class="container">
                <div class="row">
                    <div class="col-xl-6">
                        <div class="about-one__left wow slideInLeft animated" data-wow-delay="100ms" data-wow-duration="2500ms" style="visibility: visible; animation-duration: 2500ms; animation-delay: 100ms; animation-name: slideInLeft;">
                            <div class="about-one__img-box">
                                <div class="about-one__img">
                                    <img src="@item.ImageUrl" alt="@item.Title">
                                </div>
                                @if (!string.IsNullOrEmpty(item.SecondImageUrl))
                                {
                                    <div class="about-one__img-two">
                                        <img src="@item.SecondImageUrl" alt="@item.Title">
                                    </div>
                                }

                                @if (!String.IsNullOrEmpty(item.YoutubeUrlLink))
                                {
                                    <div class="about-one__video-link">
                                        <a href="@item.YoutubeUrlLink" class="video-popup">
                                            <div class="about-one__video-icon">
                                                <span class="fa fa-play">
                                                </span>
                                                <i class="ripple">
                                                </i>
                                            </div>
                                        </a>
                                    </div>
                                }
                            </div>
                            <div class="about-one__shape-1">
                            </div>
                            <div class="about-one__shape-2">
                                <img src="~/layout2025new/about-one-shape-2.png" alt="@item.Title">
                            </div>
                        </div>
                    </div>
                    <div class="col-xl-6">
                        <div class="about-one__right">
                            <div class="section-title text-left sec-title-animation animation-style2">
                                <div class="section-title__tagline-box">
                                    <span class="section-title__tagline">
                                        @item.MiniTitle
                                    </span>
                                    <div class="section-title__tagline-shape">
                                    </div>
                                </div>
                                <h2 class="section-title__title title-animation" style="perspective: 400px;">
                                    @item.Title
                                </h2>
                            </div>
                            <p class="about-one__text">
                                @Html.Raw(item.Description)
                            </p>
                            @if (item.PageSectionLists?.Count > 0)
                            {
                                <div class="about-one__points-box">

                                    <ul class="list-unstyled about-one__points">
                                        @{
                                            int i = 0;
                                            foreach (var ps in item.PageSectionLists)
                                            {
                                                if (i % 2 == 0) // even index
                                                {
                                                    <li>

                                                        <div class="about-one__points-icon">

                                                            <i class="@ps.IconText">
                                                            </i>

                                                        </div>

                                                        <div class="about-one__points-content">

                                                            <h4>
                                                                @ps.Title
                                                            </h4>

                                                            <p>
                                                                @Html.Raw(ps.Description)
                                                            </p>

                                                        </div>

                                                    </li>
                                                }
                                                i++;
                                            }
                                        }


                                    </ul>

                                    <ul class="list-unstyled about-one__points about-one__points--two">

                                        @{
                                            i = 0;
                                            foreach (var ps in item.PageSectionLists)
                                            {
                                                if (i % 2 != 0) // odd index
                                                {

                                                    <li>

                                                        <div class="about-one__points-icon">
                                                            <i class="@ps.IconText">
                                                            </i>
                                                        </div>

                                                        <div class="about-one__points-content">

                                                            <h4>
                                                                @ps.Title
                                                            </h4>

                                                            <p>
                                                                @Html.Raw(ps.Description)
                                                            </p>

                                                        </div>

                                                    </li>
                                                }
                                                i++;
                                            }
                                        }
                                    </ul>

                                </div>

                            }
                            @if (isHomePage == true)
                            {
                                if (item.ShowInHome == true && item.DisableButton == false)
                                {
                                    <div class="about-one__btn-box">
                                        @if (item.ButtonLink == "0")
                                        {
                                            <a href="#" class="thm-btn">
                                                <span class="thm-btn-text">@item.ButtonText</span>
                                                <span class="thm-btn-icon-box"><i class="fas fa-arrow-right"></i></span>
                                            </a>
                                        }
                                        else if (item.ButtonLink == "Direct")
                                        {
                                            <a href="@item.DirectUrl" class="thm-btn">
                                                <span class="thm-btn-text">@item.ButtonText</span>
                                                <span class="thm-btn-icon-box"><i class="fas fa-arrow-right"></i></span>
                                            </a>
                                        }
                                        else if (item.ButtonLink == "00")
                                        {
                                            <a asp-page="/Contact" class="thm-btn">
                                                <span class="thm-btn-text">@item.ButtonText</span>
                                                <span class="thm-btn-icon-box"><i class="fas fa-arrow-right"></i></span>
                                            </a>
                                        }
                                        else
                                        {
                                            <a asp-page="/Source"
                                               asp-route-id="@item.ButtonLink"
                                               asp-route-title="@regex.Replace(item.Title, "-")"
                                               class="thm-btn">
                                                <span class="thm-btn-text">@item.ButtonText</span>
                                                <span class="thm-btn-icon-box"><i class="fas fa-arrow-right"></i></span>
                                            </a>
                                        }
                                    </div>
                                }
                            }
                        </div>
                    </div>
                </div>
            </div>
        </section>
        <!--About One End -->
    }
    else if (item.TemplateKey == "2")
    {
        string buttonUrl = "#";

        if (item.ButtonLink == "0")
        {
            buttonUrl = "#";
        }
        else if (item.ButtonLink == "Direct")
        {
            buttonUrl = item.DirectUrl;
        }
        else if (item.ButtonLink == "00")
        {
            buttonUrl = Url.Page("/Contact");
        }
        else
        {
            buttonUrl = Url.Page("/Source", new { id = item.ButtonLink, title = regex.Replace(item.Title, "-") });
        }
        <section class="services-one @item.CustomClass">

            <div class="services-one__shape-bg float-bob-x" style="background-image: url(@item.ImageUrl);">
            </div>

            <div class="container">

                <div class="section-title text-center sec-title-animation animation-style1">

                    <div class="section-title__tagline-box">

                        <span class="section-title__tagline">
                            @item.MiniTitle
                        </span>

                        <div class="section-title__tagline-shape">
                        </div>

                    </div>

                    <h2 class="section-title__title title-animation">
                        @item.Title
                    </h2>

                </div>

                <div class="services-one__carousel owl-carousel owl-theme">
                    <!-- Item 1 -->
                    @foreach (var sectionlist in item.PageSectionLists)
                    {

                        string buttonUrlList = "#";

                        if (sectionlist.ButtonLink == "0")
                        {
                            buttonUrlList = "#";
                        }
                        else if (sectionlist.ButtonLink == "Direct")
                        {
                            buttonUrlList = sectionlist.DirectUrl;
                        }
                        else if (sectionlist.ButtonLink == "00")
                        {
                            buttonUrlList = Url.Page("/Contact");
                        }
                        else
                        {
                            buttonUrlList = Url.Page("/Source", new { id = sectionlist.ButtonLink, title = regex.Replace(sectionlist.Title, "-") });
                        }
                        <div class="item">
                            <div class="services-one__single">
                                <div class="services-one__img">
                                    <img src="@sectionlist.ImageUrl" alt="@sectionlist.Title" />
                                </div>
                                <div class="services-one__content">
                                    <div class="services-one__count">@sectionlist.SortOrder</div>
                                    <div class="services-one__icon">
                                        <i class="@sectionlist.IconText"></i>
                                    </div>
                                    <h3 class="services-one__title">
                                        <a href="@buttonUrlList">@sectionlist.Title</a>
                                    </h3>
                                    <p class="services-one__text">
                                        @sectionlist.Description
                                    </p>
                                    @if (!String.IsNullOrEmpty(sectionlist.ButtonText))
                                    {
                                        <a href="@buttonUrlList" class="services-one__btn">
                                            @sectionlist.ButtonText <span class="fas fa-arrow-right"></span>
                                        </a>
                                    }
                                </div>
                            </div>
                        </div>
                    }


                </div>


            </div>

        </section>

        <!--Services One End -->
    }
    else if (item.TemplateKey == "3")
    {
        string buttonUrl = "#";

        if (item.ButtonLink == "0")
        {
            buttonUrl = "#";
        }
        else if (item.ButtonLink == "Direct")
        {
            buttonUrl = item.DirectUrl;
        }
        else if (item.ButtonLink == "00")
        {
            buttonUrl = Url.Page("/Contact");
        }
        else
        {
            buttonUrl = Url.Page("/Source", new { id = item.ButtonLink, title = regex.Replace(item.Title, "-") });
        }
        <section class="why-choose-one @item.CustomClass">

            <div class="why-choose-one__shape-bg float-bob-y" style="background-image: url();">
            </div>

            <div class="why-choose-one__shape-1 float-bob-x">

                <img src="~/layout2025new/why-choose-one-shape-1.png" alt="@item.Title">

            </div>

            <div class="container">

                <div class="row">

                    <div class="col-xl-6">

                        <div class="why-choose-one__left">

                            <div class="section-title text-left sec-title-animation animation-style2">

                                <div class="section-title__tagline-box">

                                    <span class="section-title__tagline">
                                        @item.MiniTitle
                                    </span>

                                    <div class="section-title__tagline-shape">
                                    </div>

                                </div>

                                <h2 class="section-title__title title-animation">
                                    @item.Title
                                </h2>

                            </div>

                            <p class="why-choose-one__text">
                                @Html.Raw(item.Description)

                            </p>
                            @if (item.PageSectionLists?.Count > 0)
                            {

                                <div class="why-choose-one__points-box">
                                    <ul class="list-unstyled why-choose-one__points">
                                        @{
                                            int i = 0;
                                            foreach (var ps in item.PageSectionLists)
                                            {
                                                string buttonUrlList = "#";

                                                if (ps.ButtonLink == "0")
                                                {
                                                    buttonUrlList = "#";
                                                }
                                                else if (ps.ButtonLink == "Direct")
                                                {
                                                    buttonUrlList = ps.DirectUrl;
                                                }
                                                else if (ps.ButtonLink == "00")
                                                {
                                                    buttonUrlList = Url.Page("/Contact");
                                                }
                                                else
                                                {
                                                    buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                                                }
                                                if (i % 2 == 0) // even index
                                                {
                                                    <li>

                                                        <div class="icon">
                                                            <i class="@ps.IconText"></i>
                                                        </div>

                                                        <div class="content">

                                                            <h3>
                                                                <a href="@buttonUrlList">
                                                                    @ps.Title
                                                                </a>
                                                            </h3>

                                                            <p>
                                                                @Html.Raw(ps.Description)
                                                            </p>

                                                        </div>

                                                    </li>

                                                }
                                                i++;
                                            }
                                        }

                                    </ul>

                                    <ul class="list-unstyled why-choose-one__points why-choose-one__points--two">

                                        @{
                                            i = 0;
                                            foreach (var ps in item.PageSectionLists)
                                            {
                                                string buttonUrlList = "#";

                                                if (ps.ButtonLink == "0")
                                                {
                                                    buttonUrlList = "#";
                                                }
                                                else if (ps.ButtonLink == "Direct")
                                                {
                                                    buttonUrlList = ps.DirectUrl;
                                                }
                                                else if (ps.ButtonLink == "00")
                                                {
                                                    buttonUrlList = Url.Page("/Contact");
                                                }
                                                else
                                                {
                                                    buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                                                }
                                                if (i % 2 != 0) // odd index
                                                {
                                                    <li>

                                                        <div class="icon">
                                                            <i class="@ps.IconText"></i>
                                                        </div>

                                                        <div class="content">

                                                            <h3>
                                                                <a href="@buttonUrlList">
                                                                    @ps.Title
                                                                </a>
                                                            </h3>

                                                            <p>
                                                                @Html.Raw(ps.Description)
                                                            </p>

                                                        </div>

                                                    </li>

                                                }
                                                i++;
                                            }
                                        }

                                    </ul>

                                </div>
                            }
                            @if (item.ButtonText != null)
                            {
                                <div class="why-choose-one__btn-box">

                                    <a href="@buttonUrl" class="thm-btn">

                                        <span class="thm-btn-text">
                                            @item.ButtonText
                                        </span>

                                        <span class="thm-btn-icon-box">
                                            <i class="fas fa-arrow-right">
                                            </i>
                                        </span>

                                    </a>

                                </div>
                            }
                        </div>

                    </div>

                    <div class="col-xl-6">

                        <div class="why-choose-one__right">

                            <div class="why-choose-one__circle rotate-me">
                            </div>

                            <div class="row">

                                <div class="col-xl-6 col-lg-6 col-md-6">

                                    <div class="why-choose-one__content-one">

                                        <div class="why-choose-one__img-one">

                                            <img src="@item.ImageUrl" alt="@item.Title">

                                        </div>

                                        <div class="why-choose-one__round-text-box">

                                            <div class="why-choose-one__round-text-box-inner">

                                                <div class="why-choose-one__curved-circle" id="circleText">
                                                    @item.Qoute
                                                </div>

                                                <script>
                                                    document.addEventListener("DOMContentLoaded", function () {
                                                        const el = document.getElementById("circleText");
                                                        const text = el.textContent.trim();
                                                        el.textContent = ""; // clear original text

                                                        const radius = 6; // em, same as your transform-origin
                                                        const chars = text.split("");
                                                        const angleStep = 360 / chars.length;

                                                        chars.forEach((ch, i) => {
                                                            const span = document.createElement("span");
                                                            span.innerText = ch;
                                                            span.style.position = "absolute";
                                                            span.style.left = "50%";
                                                            span.style.transformOrigin = `center ${radius}em`;
                                                            span.style.transform = `rotate(${i * angleStep - 180}deg)`; // spread evenly
                                                            el.appendChild(span);
                                                        });
                                                    });
                                                </script>


                                            </div>

                                        </div>

                                    </div>

                                </div>

                                <div class="col-xl-6 col-lg-6 col-md-6">

                                    <div class="why-choose-one__content-two">

                                        <div class="why-choose-one__icon">

                                            <i class="fas fa-globe">
                                            </i>

                                        </div>

                                        <div class="why-choose-one__img-two">

                                            <img src="@item.SecondImageUrl" alt="@item.Title">

                                        </div>

                                    </div>

                                </div>

                            </div>

                        </div>

                    </div>

                </div>

            </div>

        </section>

        <!--Services One End -->
    }
    else if (item.TemplateKey == "4")
    {
        string buttonUrl = "#";

        if (item.ButtonLink == "0")
        {
            buttonUrl = "#";
        }
        else if (item.ButtonLink == "Direct")
        {
            buttonUrl = item.DirectUrl;
        }
        else if (item.ButtonLink == "00")
        {
            buttonUrl = Url.Page("/Contact");
        }
        else
        {
            buttonUrl = Url.Page("/Source", new { id = item.ButtonLink, title = regex.Replace(item.Title, "-") });
        }
        <section class="why-choose-one @item.CustomClass">

            <div class="why-choose-one__shape-bg float-bob-y" style="background-image: url();">
            </div>

            <div class="why-choose-one__shape-1 float-bob-x">

                <img src="~/layout2025new/why-choose-one-shape-1.png" alt="@item.Title">

            </div>

            <div class="container">

                <div class="row">

                    <div class="col-xl-6">

                        <div class="why-choose-one__left">

                            <div class="section-title text-left sec-title-animation animation-style2">

                                <div class="section-title__tagline-box">

                                    <span class="section-title__tagline">
                                        @item.MiniTitle
                                    </span>

                                    <div class="section-title__tagline-shape">
                                    </div>

                                </div>

                                <h2 class="section-title__title title-animation">
                                    @item.Title
                                </h2>

                            </div>

                            <p class="why-choose-one__text">
                                @Html.Raw(item.Description)

                            </p>
                            @if (item.PageSectionLists?.Count > 0)
                            {

                                <div class="why-choose-one__points-box">
                                    <ul class="list-unstyled why-choose-one__points">
                                        @{
                                            int i = 0;
                                            foreach (var ps in item.PageSectionLists)
                                            {
                                                string buttonUrlList = "#";

                                                if (ps.ButtonLink == "0")
                                                {
                                                    buttonUrlList = "#";
                                                }
                                                else if (ps.ButtonLink == "Direct")
                                                {
                                                    buttonUrlList = ps.DirectUrl;
                                                }
                                                else if (ps.ButtonLink == "00")
                                                {
                                                    buttonUrlList = Url.Page("/Contact");
                                                }
                                                else
                                                {
                                                    buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                                                }
                                                if (i % 2 == 0) // even index
                                                {
                                                    <li>

                                                        <div class="icon">
                                                            <i class="@ps.IconText"></i>
                                                        </div>

                                                        <div class="content">

                                                            <h3>
                                                                <a href="@buttonUrlList">
                                                                    @ps.Title
                                                                </a>
                                                            </h3>

                                                            <p>
                                                                @Html.Raw(ps.Description)
                                                            </p>

                                                        </div>

                                                    </li>

                                                }
                                                i++;
                                            }
                                        }

                                    </ul>

                                    <ul class="list-unstyled why-choose-one__points why-choose-one__points--two">

                                        @{
                                            i = 0;
                                            foreach (var ps in item.PageSectionLists)
                                            {
                                                string buttonUrlList = "#";

                                                if (ps.ButtonLink == "0")
                                                {
                                                    buttonUrlList = "#";
                                                }
                                                else if (ps.ButtonLink == "Direct")
                                                {
                                                    buttonUrlList = ps.DirectUrl;
                                                }
                                                else if (ps.ButtonLink == "00")
                                                {
                                                    buttonUrlList = Url.Page("/Contact");
                                                }
                                                else
                                                {
                                                    buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                                                }
                                                if (i % 2 != 0) // odd index
                                                {
                                                    <li>

                                                        <div class="icon">
                                                            <i class="@ps.IconText"></i>
                                                        </div>

                                                        <div class="content">

                                                            <h3>
                                                                <a href="@buttonUrlList">
                                                                    @ps.Title
                                                                </a>
                                                            </h3>

                                                            <p>
                                                                @Html.Raw(ps.Description)
                                                            </p>

                                                        </div>

                                                    </li>

                                                }
                                                i++;
                                            }
                                        }

                                    </ul>

                                </div>
                            }
                            @if (item.ButtonText != null)
                            {
                                <div class="why-choose-one__btn-box">

                                    <a href="@buttonUrl" class="thm-btn">

                                        <span class="thm-btn-text">
                                            @item.ButtonText
                                        </span>

                                        <span class="thm-btn-icon-box">
                                            <i class="fas fa-arrow-right">
                                            </i>
                                        </span>

                                    </a>

                                </div>
                            }

                        </div>

                    </div>

                    <div class="col-xl-6">

                        <img src="@item.ImageUrl" alt="@item.Title">

                    </div>

                </div>

            </div>

        </section>

        <!--Services One End -->
    }
    else if (item.TemplateKey == "5")
    {
        string buttonUrl = "#";

        if (item.ButtonLink == "0")
        {
            buttonUrl = "#";
        }
        else if (item.ButtonLink == "Direct")
        {
            buttonUrl = item.DirectUrl;
        }
        else if (item.ButtonLink == "00")
        {
            buttonUrl = Url.Page("/Contact");
        }
        else
        {
            buttonUrl = Url.Page("/Source", new { id = item.ButtonLink, title = regex.Replace(item.Title, "-") });
        }

        <section class="skill-one @item.CustomClass">

            <div class="skill-one__shape-1">

                <img src="" alt="@item.Title">

            </div>

            <div class="container">

                <div class="row">

                    <div class="col-xl-6 col-lg-6">

                        <div class="skill-one__left">

                            <div class="section-title text-left sec-title-animation animation-style2">

                                <div class="section-title__tagline-box">

                                    <span class="section-title__tagline">
                                        @item.MiniTitle
                                    </span>

                                    <div class="section-title__tagline-shape">
                                    </div>

                                </div>

                                <h2 class="section-title__title title-animation" style="perspective: 400px;">
                                    @item.Title
                                </h2>

                            </div>

                            <p class="skill-one__text">
                                @Html.Raw(item.Description)
                            </p>

                            <div class="skill-one__progress-box">

                                @foreach (var ps in item.PageSectionLists)
                                {
                                    <div class="progress-box">

                                        <div class="bar-title">
                                            @ps.Title
                                        </div>

                                        <div class="bar">

                                            <div class="bar-inner count-bar counted" data-percent="@ps.MiniTitle%" style="width: @ps.MiniTitle%;">

                                                <div class="count-box counted">

                                                    <span class="count-text" data-stop="@ps.MiniTitle" data-speed="1500">
                                                        @ps.MiniTitle
                                                    </span>%

                                                </div>

                                            </div>

                                        </div>

                                    </div>
                                }

                            </div>

                        </div>

                    </div>

                    <div class="col-xl-6 col-lg-6">

                        <div class="skill-one__right">

                            <div class="skill-one__right-img-box wow slideInRight animated" data-wow-delay="100ms" data-wow-duration="2500ms" style="visibility: visible; animation-duration: 2500ms; animation-delay: 100ms; animation-name: slideInRight;">

                                <div class="skill-one__right-img">

                                    <img src="@item.ImageUrl" alt="@item.Title">

                                    <div class="skill-one__video-link">

                                        <a href="@item.YoutubeUrlLink" class="video-popup">

                                            <div class="skill-one__video-icon">

                                                <span class="fa fa-play">
                                                </span>

                                                <i class="ripple">
                                                </i>

                                            </div>

                                        </a>

                                    </div>

                                </div>

                                <div class="skill-one__video-content">

                                    <p>
                                        @Html.Raw(item.Qoute)
                                    </p>

                                </div>

                            </div>

                        </div>

                    </div>

                </div>

            </div>

        </section>


        <!--Services One End -->
    }
    else if (item.TemplateKey == "6")
    {
        string buttonUrl = "#";

        if (item.ButtonLink == "0")
        {
            buttonUrl = "#";
        }
        else if (item.ButtonLink == "Direct")
        {
            buttonUrl = item.DirectUrl;
        }
        else if (item.ButtonLink == "00")
        {
            buttonUrl = Url.Page("/Contact");
        }
        else
        {
            buttonUrl = Url.Page("/Source", new { id = item.ButtonLink, title = regex.Replace(item.Title, "-") });
        }

        <section class="process-one @item.CustomClass">


            <div class="container">

                <div class="section-title text-center sec-title-animation animation-style1">

                    <div class="section-title__tagline-box">

                        <span class="section-title__tagline">
                            @item.MiniTitle
                        </span>

                        <div class="section-title__tagline-shape">
                        </div>

                    </div>

                    <h2 class="section-title__title title-animation" style="perspective: 400px;">
                        @item.Title
                    </h2>
                    <p>
                        @Html.Raw(item.Description)
                    </p>
                </div>
                @{
                    var all = item?.PageSectionLists?.Take(4).ToList() ?? new List<PageSectionList>();

                    var firstOne = all.Count > 0 ? all[0] : null;
                    var secondOne = all.Count > 1 ? all[1] : null;
                    var thirdOne = all.Count > 2 ? all[2] : null;
                    var fourthOne = all.Count > 3 ? all[3] : null;
                }

                <div class="process-one__inner">

                    <ul class="list-unstyled row">

                        <!--Process Two Single Start -->
                        @if (firstOne != null)
                        {
                            <li class="col-xl-3 col-lg-6 col-md-6 wow fadeInLeft animated" data-wow-delay="100ms" style="visibility: visible; animation-delay: 100ms; animation-name: fadeInLeft;">

                                <div class="process-one__single">

                                    <div class="process-one__shape-1">
                                    </div>

                                    <div class="process-one__icon-box">

                                        <div class="process-one__count">
                                        </div>

                                        <div class="process-one__icon">

                                            <i class="@firstOne.IconText">
                                            </i>

                                        </div>

                                    </div>

                                    <h3 class="process-one__title">
                                        @firstOne.Title
                                    </h3>

                                    <p class="process-one__text">
                                        @firstOne.Description
                                    </p>

                                </div>

                            </li>

                            <!--Process Two Single End -->
                        } <!--Process Two Single Start -->
                        @if (secondOne != null)
                        {
                            <li class="col-xl-3 col-lg-6 col-md-6 wow fadeInRight animated" data-wow-delay="100ms" style="visibility: visible; animation-delay: 100ms; animation-name: fadeInRight;">

                                <div class="process-one__single">

                                    <div class="process-one__shape-2">
                                    </div>

                                    <div class="process-one__icon-box">

                                        <div class="process-one__count">
                                        </div>

                                        <div class="process-one__icon">

                                            <i class="@secondOne.IconText">
                                            </i>

                                        </div>

                                    </div>

                                    <h3 class="process-one__title">
                                        @secondOne.Title
                                    </h3>

                                    <p class="process-one__text">
                                        @secondOne.Description
                                    </p>

                                </div>

                            </li>

                            <!--Process Two Single End -->
                        }
                        @if (thirdOne != null)
                        {<!--Process Two Single Start -->

                            <li class="col-xl-3 col-lg-6 col-md-6 wow fadeInLeft animated" data-wow-delay="100ms" style="visibility: visible; animation-delay: 100ms; animation-name: fadeInLeft;">

                                <div class="process-one__single">

                                    <div class="process-one__shape-3">
                                    </div>

                                    <div class="process-one__icon-box">

                                        <div class="process-one__count">
                                        </div>

                                        <div class="process-one__icon">

                                            <i class="@thirdOne.IconText">
                                            </i>

                                        </div>

                                    </div>

                                    <h3 class="process-one__title">
                                        @thirdOne.Title
                                    </h3>

                                    <p class="process-one__text">
                                        @thirdOne.Description
                                    </p>

                                </div>

                            </li>

                            <!--Process Two Single End -->
                        }
                        @if (fourthOne != null)
                        {<!--Process Two Single Start -->

                            <li class="col-xl-3 col-lg-6 col-md-6 wow fadeInRight animated" data-wow-delay="100ms" style="visibility: visible; animation-delay: 100ms; animation-name: fadeInRight;">

                                <div class="process-one__single">

                                    <div class="process-one__icon-box">

                                        <div class="process-one__count">
                                        </div>

                                        <div class="process-one__icon">

                                            <i class="@fourthOne.IconText">
                                            </i>

                                        </div>

                                    </div>

                                    <h3 class="process-one__title">
                                        @fourthOne.Title
                                    </h3>

                                    <p class="process-one__text">
                                        @fourthOne.Description

                                    </p>

                                </div>

                            </li>
                        }
                        <!--Process Two Single End -->

                    </ul>

                </div>

            </div>

        </section>

    }
    else if (item.TemplateKey == "7")
    {

        <section class="sliding-text-one @item.CustomClass">
            <div class="sliding-text-one__wrap">
                <ul class="sliding-text-one__list list-unstyled marquee_mode">
                    @foreach (var ps in item.PageSectionLists)
                    {

                        <li><h2 data-hover="@ps.Title" class="sliding-text-one__title">@ps.Title</h2></li>
                        <li><span></span></li>
                    }

                </ul>

            </div>
        </section>

    }
    else if (item.TemplateKey == "8")
    {

        <section class="services-two @item.CustomClass">

            <div class="services-two__bg jarallax" data-jarallax="" data-speed="0.2" data-imgposition="50% 0%" style="background-image: none;" data-jarallax-original-styles="background-image: url(@item.ImageUrl);">
                <div id="jarallax-container-0" style="position: absolute; top: 0px; left: 0px; width: 100%; height: 100%; overflow: hidden; z-index: -100;">
                    <div style="background-position: 50% 50%; background-size: cover; background-repeat: no-repeat; background-image: url('@item.ImageUrl'); position: fixed; top: 0px; left: 0px; width: 1790.4px; height: 1059.8px; overflow: hidden; pointer-events: none; transform-style: preserve-3d; backface-visibility: hidden; will-change: transform, opacity; margin-top: 5.1px; transform: translate3d(0px, 7.18px, 0px);">
                    </div>
                </div>
            </div>

            <div class="container">

                <div class="section-title text-center sec-title-animation animation-style1">

                    <div class="section-title__tagline-box">

                        <span class="section-title__tagline">
                            @item.MiniTitle
                        </span>

                        <div class="section-title__tagline-shape">
                        </div>

                    </div>

                    <h2 class="section-title__title title-animation" style="perspective: 400px;">
                        @item.Title
                    </h2>
                    <p>
                        @Html.Raw(item.Description)

                    </p>
                </div>

                <div class="row">

                    <!--Services Two Single Start -->
                    @foreach (var ps in item.PageSectionLists)
                    {
                        string buttonUrlList = "#";

                        if (ps.ButtonLink == "0")
                        {
                            buttonUrlList = "#";
                        }
                        else if (ps.ButtonLink == "Direct")
                        {
                            buttonUrlList = ps.DirectUrl;
                        }
                        else if (ps.ButtonLink == "00")
                        {
                            buttonUrlList = Url.Page("/Contact");
                        }
                        else
                        {
                            buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                        }
                        <div class="col-xl-4 col-lg-6 col-md-6 wow fadeInLeft animated" data-wow-delay="100ms" style="visibility: visible; animation-delay: 100ms; animation-name: fadeInLeft;">

                            <div class="services-two__single">

                                <div class="services-two__icon">

                                    <i class="@ps.IconText">
                                    </i>

                                </div>

                                <h3 class="services-two__title">
                                    <a href="@buttonUrlList">
                                        @ps.Title
                                    </a>
                                </h3>

                                <p class="services-two__text">
                                    @Html.Raw(ps.Description)
                                </p>

                                <a href="@buttonUrlList" class="services-two__read-more">
                                    @ps.ButtonText
                                    <span class="icon-right-arrow">
                                    </span>
                                </a>

                            </div>

                        </div>
                    }

                </div>

            </div>

        </section>
    }
    else if (item.TemplateKey == "9")
    {
        <section class="team-one @item.CustomClass">
            <div class="container">
                <div class="section-title text-left sec-title-animation animation-style2">
                    <div class="section-title__tagline-box">
                        <span class="section-title__tagline">@item.MiniTitle</span>
                        <div class="section-title__tagline-shape"></div>
                    </div>
                    <h2 class="section-title__title title-animation">@item.Title</h2>
                </div>

                <div class="team-one__carousel owl-theme owl-carousel">
                    @foreach (var ps in item.PageSectionLists)
                    {
                        string buttonUrlList = "#";

                        if (ps.ButtonLink == "0")
                        {
                            buttonUrlList = "#";
                        }
                        else if (ps.ButtonLink == "Direct")
                        {
                            buttonUrlList = ps.DirectUrl;
                        }
                        else if (ps.ButtonLink == "00")
                        {
                            buttonUrlList = Url.Page("/Contact");
                        }
                        else
                        {
                            buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                        }
                        <div class="item">
                            <div class="team-one__single">
                                <div class="team-one__content">
                                    <h3 class="team-one__name"><a href="@buttonUrlList">@ps.Title</a></h3>
                                    <p class="team-one__sub-title">@ps.MiniTitle</p>
                                </div>
                                <div class="team-one__img-box">
                                    <div class="team-one__img">
                                        <img src="@ps.ImageUrl" alt="@item.Title">
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </section>

    }
    else if (item.TemplateKey == "10")
    {
        <section class="testimonial-two @item.CustomClass">
            <div class="testimonial-two__shape-1 float-bob-y">
                <img src="~/layout2025new/testimonial-two-shape-1.png" alt="@item.Title">
            </div>
            <div class="testimonial-two__shape-2 float-bob-x">
                <img src="~/layout2025new/testimonial-two-shape-2.png" alt="@item.Title">
            </div>
            <div class="container">
                <div class="section-title text-center sec-title-animation animation-style1">
                    <div class="section-title__tagline-box">
                        <span class="section-title__tagline">@item.MiniTitle</span>
                        <div class="section-title__tagline-shape"></div>
                    </div>
                    <h2 class="section-title__title">@item.Title</h2>
                </div>

                <div class="testimonial-two__carousel owl-carousel owl-theme">
                    @foreach (var ps in item.PageSectionLists)
                    {
                        string buttonUrlList = "#";

                        if (ps.ButtonLink == "0")
                        {
                            buttonUrlList = "#";
                        }
                        else if (ps.ButtonLink == "Direct")
                        {
                            buttonUrlList = ps.DirectUrl;
                        }
                        else if (ps.ButtonLink == "00")
                        {
                            buttonUrlList = Url.Page("/Contact");
                        }
                        else
                        {
                            buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                        }
                        <!-- Item 1 -->
                        <div class="testimonial-two__single">
                            <div class="testimonial-two__quote">
                                <i class="@ps.IconText"></i>
                            </div>
                            <div class="testimonial-two__img">
                                <img src="@ps.ImageUrl" alt="@item.Title">
                            </div>
                            <p class="testimonial-two__text">@ps.Description</p>
                            <div class="testimonial-two__client-info">
                                <div class="testimonial-two__client-content">
                                    <h4 class="testimonial-two__client-name"><a href="@buttonUrlList">@ps.MiniTitle</a></h4>
                                    <p class="testimonial-two__client-sub-title">@ps.MoreDescription</p>
                                </div>
                                <div class="testimonial-two__rating">
                                    <span class="icon-star"></span>
                                    <span class="icon-star"></span>
                                    <span class="icon-star"></span>
                                    <span class="icon-star"></span>
                                    <span class="icon-star"></span>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </section>

    }
    else if (item.TemplateKey == "11")
    {
        <section class="testimonial-one @item.CustomClass">
            <div class="testimonial-one__bg float-bob-y" style="background-image: url(@item.ImageUrl);"></div>
            <div class="container">
                <div class="row">

                    <!-- Left Column -->
                    <div class="col-xl-5 col-lg-5">
                        <div class="testimonial-one__left">
                            <div class="section-title text-left sec-title-animation animation-style2">
                                <div class="section-title__tagline-box">
                                    <span class="section-title__tagline">@item.MiniTitle</span>
                                    <div class="section-title__tagline-shape"></div>
                                </div>
                                <h2 class="section-title__title">@item.Title</h2>
                            </div>
                            <div class="testimonial-one__satisfied-clients">

                                <p class="testimonial-one__satisfied-clients-text">
                                    @Html.Raw(item.Description)

                                </p>
                            </div>

                        </div>
                    </div>

                    <!-- Right Column -->
                    <div class="col-xl-7 col-lg-7">
                        <div class="testimonial-one__right">
                            <div class="testimonial-one__carousel owl-carousel owl-theme">
                                @foreach (var ps in item.PageSectionLists)
                                {
                                    string buttonUrlList = "#";

                                    if (ps.ButtonLink == "0")
                                    {
                                        buttonUrlList = "#";
                                    }
                                    else if (ps.ButtonLink == "Direct")
                                    {
                                        buttonUrlList = ps.DirectUrl;
                                    }
                                    else if (ps.ButtonLink == "00")
                                    {
                                        buttonUrlList = Url.Page("/Contact");
                                    }
                                    else
                                    {
                                        buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                                    }
                                    <!-- Item 1 -->
                                    <div class="testimonial-one__single">
                                        <div class="testimonial-one__quote"><span class="icon-quote"></span></div>
                                         
                                        <div class="testimonial-one__client-info">
                                            <div class="testimonial-one__client-content">
                                                <img src="@ps.ImageUrl" alt="@ps.Title">
                                                <h4 class="testimonial-one__client-name"><a href="@buttonUrlList">@ps.Title</a></h4>
                                                <p class="testimonial-one__client-sub-title">@ps.MiniTitle</p>
                                            </div>

                                        </div>
                                        <p class="testimonial-one__text">
                                            @ps.Description
                                        </p>
                                    </div>

                                }
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </section>

    }
    else if (item.TemplateKey == "12")
    {
        string buttonUrl = "#";

        if (item.ButtonLink == "0")
        {
            buttonUrl = "#";
        }
        else if (item.ButtonLink == "Direct")
        {
            buttonUrl = item.DirectUrl;
        }
        else if (item.ButtonLink == "00")
        {
            buttonUrl = Url.Page("/Contact");
        }
        else
        {
            buttonUrl = Url.Page("/Source", new { id = item.ButtonLink, title = regex.Replace(item.Title, "-") });
        }
        <section class="case-one @item.CustomClass">
            <div class="case-one__top">
                <div class="container">
                    <div class="case-one__top-inner">
                        <div class="section-title text-left sec-title-animation animation-style2">
                            <div class="section-title__tagline-box">
                                <span class="section-title__tagline">@item.MiniTitle</span>
                                <div class="section-title__tagline-shape"></div>
                            </div>
                            <h2 class="section-title__title">@item.Title</h2>
                            <p>@Html.Raw(item.Description)</p>
                        </div>
                        <div class="case-one__btn-box">
                            <a href="@buttonUrl" class="thm-btn">
                                <span class="thm-btn-text">@item.ButtonText</span>
                                <span class="thm-btn-icon-box"><i class="fas fa-arrow-right"></i></span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <div class="case-one__bottom">
                <div class="container">
                    <div class="case-one__carousel owl-carousel owl-theme">
                        @foreach (var ps in item.PageSectionLists)
                        {
                            string buttonUrlList = "#";

                            if (ps.ButtonLink == "0")
                            {
                                buttonUrlList = "#";
                            }
                            else if (ps.ButtonLink == "Direct")
                            {
                                buttonUrlList = ps.DirectUrl;
                            }
                            else if (ps.ButtonLink == "00")
                            {
                                buttonUrlList = Url.Page("/Contact");
                            }
                            else
                            {
                                buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                            }
                            <!-- Item 1 -->
                            <div class="case-one__single">
                                <div class="case-one__single-inner">
                                    <div class="case-one__img-box">
                                        <div class="case-one__img">
                                            <img src="@ps.ImageUrl" alt="@item.Title">
                                        </div>
                                    </div>
                                    <div class="case-one__content">
                                        <div class="case-one__content-box">
                                            <h3><a href="@buttonUrlList">@ps.Title</a></h3>
                                            <p>@ps.MiniTitle</p>
                                        </div>
                                        <div class="case-one__icon-box">
                                            <a href="@buttonUrlList" class="img-popup">
                                                <span class="icon-right-arrow-1"></span>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }

                    </div><!-- /.case-one__carousel -->
                </div>
            </div>
        </section>
    }
    else if (item.TemplateKey == "13")
    {
        string buttonUrl = "#";

        if (item.ButtonLink == "0")
        {
            buttonUrl = "#";
        }
        else if (item.ButtonLink == "Direct")
        {
            buttonUrl = item.DirectUrl;
        }
        else if (item.ButtonLink == "00")
        {
            buttonUrl = Url.Page("/Contact");
        }
        else
        {
            buttonUrl = Url.Page("/Source", new { id = item.ButtonLink, title = regex.Replace(item.Title, "-") });
        }

        <section class="counter-one @item.CustomClass">
            <div class="counter-one__bg-box">
                <div class="counter-one__bg jarallax"
                     data-jarallax
                     data-speed="0.2"
                     data-imgposition="50% 0%"
                     style="background-image: url(@item.ImageUrl);">
                </div>
            </div>

            <div class="container">
                <div class="counter-one__top">
                    <div class="section-title text-left sec-title-animation animation-style2">
                        <div class="section-title__tagline-box">
                            <span class="section-title__tagline">@item.MiniTitle</span>
                            <div class="section-title__tagline-shape"></div>
                        </div>
                        <h2 class="section-title__title">@item.Title</h2>
                    </div>
                    <div class="counter-one__btn-box">
                        <a href="@buttonUrl" class="thm-btn">
                            <span class="thm-btn-text">@item.ButtonText</span>
                            <span class="thm-btn-icon-box"><i class="fas fa-arrow-right"></i></span>
                        </a>
                    </div>
                </div>

                <div class="counter-one__bottom">
                    <div class="row">
                        @foreach (var ps in item.PageSectionLists)
                        {
                            string buttonUrlList = "#";

                            if (ps.ButtonLink == "0")
                            {
                                buttonUrlList = "#";
                            }
                            else if (ps.ButtonLink == "Direct")
                            {
                                buttonUrlList = ps.DirectUrl;
                            }
                            else if (ps.ButtonLink == "00")
                            {
                                buttonUrlList = Url.Page("/Contact");
                            }
                            else
                            {
                                buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                            }

                            <!-- Counter 1 -->
                            <div class="col-xl-3 col-lg-6 col-md-6 wow fadeInLeft" data-wow-delay="100ms">
                                <div class="counter-one__single">
                                    <div class="counter-one__icon"><i class="@ps.IconText"></i></div>
                                    <div class="counter-one__content">
                                        <div class="counter-one__count-box">
                                            <h3 class="">@ps.MiniTitle<span class="numbertext">@ps.Description</span> </h3>
                                        </div>
                                        <p class="counter-one__text">@ps.Title</p>
                                    </div>
                                </div>
                            </div>

                        }
                    </div>
                </div>
            </div>
        </section>



    }
    else if (item.TemplateKey == "14")
    {
        <section class="team-one @item.CustomClass">
            <div class="container">
                <div class="section-title text-left sec-title-animation animation-style2">
                    <div class="section-title__tagline-box">
                        <span class="section-title__tagline">@item.MiniTitle</span>
                        <div class="section-title__tagline-shape"></div>
                    </div>
                    <h2 class="section-title__title">@item.Title</h2>
                </div>

                <div class="team-one__carousel owl-theme owl-carousel">
                    @foreach (var ps in item.PageSectionLists)
                    {
                        string buttonUrlList = "#";

                        if (ps.ButtonLink == "0")
                        {
                            buttonUrlList = "#";
                        }
                        else if (ps.ButtonLink == "Direct")
                        {
                            buttonUrlList = ps.DirectUrl;
                        }
                        else if (ps.ButtonLink == "00")
                        {
                            buttonUrlList = Url.Page("/Contact");
                        }
                        else
                        {
                            buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                        }
                        <!-- Team Member 1 -->
                        <div class="item">
                            <div class="team-one__single">
                                <div class="team-one__content">
                                    <div class="team-one__shape-box-2">
                                        <div class="team-one__shape-2"></div>
                                    </div>
                                    <h3 class="team-one__name"><a href="@buttonUrlList">@ps.Title</a></h3>
                                    <p class="team-one__sub-title">@ps.MiniTitle</p>

                                </div>
                                <div class="team-one__img-box">
                                    <div class="team-one__shape-box"><div class="team-one__shape-1"></div></div>
                                    <div class="team-one__img">
                                        <img src="@ps.ImageUrl" alt="@item.Title">
                                    </div>
                                </div>
                            </div>
                        </div>
                    }

                </div>
            </div>
        </section>

    }
    else if (item.TemplateKey == "15")
    {


        <section class="video-one @item.CustomClass">

            <div class="video-one__bg-color">

                <div class="video-one__bg jarallax" data-jarallax="" data-speed="0.2" data-imgposition="50% 0%" style="background-image: none;" data-jarallax-original-styles="background-image: url(@item.ImageUrl);">

                    <div id="jarallax-container-1" style="position: absolute; top: 0px; left: 0px; width: 100%; height: 100%; overflow: hidden; z-index: -100;">

                        <div style="background-position: 50% 50%; background-size: cover; background-repeat: no-repeat; background-image: url(@item.ImageUrl); position: fixed; top: 0px; left: 0px; width: 1790.4px; height: 978.468px; overflow: hidden; pointer-events: none; transform-style: preserve-3d; backface-visibility: hidden; will-change: transform, opacity; margin-top: 45.7662px; transform: translate3d(0px, -19.4887px, 0px);">
                        </div>
                    </div>

                </div>

            </div>

            <div class="container">

                <div class="video-one__inner">

                    <div class="section-title text-center sec-title-animation animation-style1">

                        <div class="section-title__tagline-box">

                            <span class="section-title__tagline">
                                @item.MiniTitle
                            </span>

                            <div class="section-title__tagline-shape">
                            </div>

                        </div>

                        <h2 class="section-title__title title-animation" style="perspective: 400px;">
                            @item.Title
                        </h2>

                    </div>


                    <p class="video-one__text">
                        @Html.Raw(item.Description)

                    </p>

                    <div class="video-one__video-link">

                        <a href="@item.YoutubeUrlLink" class="video-popup">

                            <div class="video-one__video-icon">

                                <span class="fa fa-play">
                                </span>

                                <i class="ripple">
                                </i>

                            </div>

                        </a>

                    </div>

                    <div class="video-one__contact-box">

                        <div class="video-one__img-1">

                            <img src="@item.ImageUrl" alt="@item.Title">

                        </div>

                        <div class="row">


                            <div class="col-xl-12 col-lg-12">

                                <div class="video-one-contact__info-box">

                                    <h3 class="video-one-contact__info-title">
                                        @item.Qoute
                                    </h3>

                                    <p>
                                        @Html.Raw(item.FullDescription)
                                    </p>

                                </div>

                            </div>

                        </div>

                    </div>

                </div>

            </div>

        </section>

    }
    else if (item.TemplateKey == "16")
    {
        <section class="faq-one @item.CustomClass">
            <div class="container">
                <div class="row">

                    <!-- Left Column -->
                    <div class="col-xl-5 col-lg-5 wow fadeInLeft" data-wow-delay="100ms">
                        <div class="faq-one__left">
                            <div class="section-title text-left sec-title-animation animation-style2">
                                <div class="section-title__tagline-box justify-content-center">
                                    <div class="section-title__tagline-shape"></div>
                                    <span class="section-title__tagline">@item.MiniTitle</span>
                                    <div class="section-title__tagline-shape"></div>
                                </div>
                                <h2 class="section-title__title">@item.Title</h2>
                            </div>
                            <div class="faq-one__img-box">
                                <div class="faq-one__img">
                                    <img src="@item.ImageUrl" alt="@item.Title">
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Right Column -->
                    <div class="col-xl-7 col-lg-7 wow fadeInRight" data-wow-delay="200ms">
                        <div class="faq-one__right">
                            <div class="accrodion-grp faq-one-accrodion" data-grp-name="faq-one-accrodion">
                                @foreach (var ps in item.PageSectionLists)
                                {
                                    string buttonUrlList = "#";

                                    if (ps.ButtonLink == "0")
                                    {
                                        buttonUrlList = "#";
                                    }
                                    else if (ps.ButtonLink == "Direct")
                                    {
                                        buttonUrlList = ps.DirectUrl;
                                    }
                                    else if (ps.ButtonLink == "00")
                                    {
                                        buttonUrlList = Url.Page("/Contact");
                                    }
                                    else
                                    {
                                        buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                                    }
                                    <!-- FAQ 1 -->
                                    <div class="accrodion">
                                        <div class="accrodion-title">
                                            <h4>@ps.Title</h4>
                                        </div>
                                        <div class="accrodion-content">
                                            <div class="inner">
                                                <p>@Html.Raw(ps.Description)</p>
                                            </div>
                                        </div>
                                    </div>
                                }

                            </div><!-- /.accrodion-grp -->
                        </div>
                    </div>

                </div>
            </div>
        </section>

    }
    else if (item.TemplateKey == "17")
    {
        <section class="pricing-one @item.CustomClass">
            <div class="pricing-one__shape-1 img-bounce">
                <img src="@item.ImageUrl" alt="@item.Title">
            </div>
            <div class="pricing-one__shape-2 rotate-me">
                <img src="@item.SecondImageUrl" alt="@item.Title" class="float-bob-y">
            </div>

            <div class="container">
                <!-- Section Title -->
                <div class="section-title text-center sec-title-animation animation-style1">
                    <div class="section-title__tagline-box justify-content-center">
                        <div class="section-title__tagline-shape"></div>
                        <span class="section-title__tagline">@item.MiniTitle</span>
                        <div class="section-title__tagline-shape"></div>
                    </div>
                    <h2 class="section-title__title">@item.Title</h2>
                </div>

                <!-- Pricing Row -->
                <div class="pricing-one__inner">
                    <div class="row">
                        @foreach (var ps in item.PageSectionLists)
                        {
                            string buttonUrlList = "#";

                            if (ps.ButtonLink == "0")
                            {
                                buttonUrlList = "#";
                            }
                            else if (ps.ButtonLink == "Direct")
                            {
                                buttonUrlList = ps.DirectUrl;
                            }
                            else if (ps.ButtonLink == "00")
                            {
                                buttonUrlList = Url.Page("/Contact");
                            }
                            else
                            {
                                buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                            }
                            <div class="col-xl-4 col-lg-4">
                                <div class="pricing-one__single">
                                    <div class="pricing-one__single-inner">

                                        <div class="pricing-one__price-box">
                                            <p class="pricing-one__price-title">@ps.Title</p>
                                            <h3 class="pricing-one__price">@ps.MiniTitle</h3>
                                            <p class="pricing-one__text">@ps.Description</p>
                                        </div>
                                        <ul class="list-unstyled pricing-one__points-list">
                                            @Html.Raw(ps.MoreDescription)
                                        </ul>
                                        <div class="pricing-one__btn-box">
                                            <a href="@buttonUrlList" class="thm-btn">
                                                <span class="thm-btn-text">@ps.ButtonText</span>
                                                <span class="thm-btn-icon-box"><i class="fas fa-arrow-right"></i></span>
                                            </a>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }

                    </div><!-- /.row -->
                </div><!-- /.pricing-one__inner -->

            </div>
        </section>


    }
    else if (item.TemplateKey == "18")
    {
        <section class="brand-one @item.CustomClass">
            <div class="container">
                <div class="row align-items-center">

                    <!-- Left Text -->
                    <div class="col-xl-3 col-lg-4 col-md-5">
                        <div class="brand-one__left">
                            <p class="brand-one__text">
                                @item.Title
                            </p>
                        </div>
                    </div>

                    <!-- Right Logos -->
                    <div class="col-xl-9 col-lg-8 col-md-7">
                        <div class="brand-one__right">
                            <div class="brand-one__carousel owl-theme owl-carousel">
                                @foreach (var ps in item.PageSectionLists)
                                {
                                    string buttonUrlList = "#";

                                    if (ps.ButtonLink == "0")
                                    {
                                        buttonUrlList = "#";
                                    }
                                    else if (ps.ButtonLink == "Direct")
                                    {
                                        buttonUrlList = ps.DirectUrl;
                                    }
                                    else if (ps.ButtonLink == "00")
                                    {
                                        buttonUrlList = Url.Page("/Contact");
                                    }
                                    else
                                    {
                                        buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                                    }
                                    <!-- Brand Item -->
                                    <div class="brand-one__single">
                                        <div class="brand-one__img">
                                            <a href="@buttonUrlList"><img src="@ps.ImageUrl" alt="Brand 1"></a>
                                        </div>
                                    </div>
                                }

                            </div><!-- /.brand-one__carousel -->
                        </div>
                    </div>

                </div>
            </div>
        </section>

    }
    else if (item.TemplateKey == "19")
    {

        <section class="blog-one @item.CustomClass">
            <div class="blog-one__shape-1 img-bounce">
                <img src="@item.ImageUrl" alt="@item.Title">
            </div>
            <div class="container">
                <!-- Section Title -->
                <div class="section-title text-left sec-title-animation animation-style2">
                    <div class="section-title__tagline-box justify-content-center">
                        <div class="section-title__tagline-shape"></div>
                        <span class="section-title__tagline">@item.MiniTitle</span>
                        <div class="section-title__tagline-shape"></div>
                    </div>
                    <h2 class="section-title__title title-animation" style="perspective:400px;">
                        @item.Title
                    </h2>
                </div>

                <!-- Blog Carousel -->
                <div class="blog-one__carousel owl-carousel owl-theme">
                    @foreach (var ps in item.PageSectionLists)
                    {
                        string buttonUrlList = "#";

                        if (ps.ButtonLink == "0")
                        {
                            buttonUrlList = "#";
                        }
                        else if (ps.ButtonLink == "Direct")
                        {
                            buttonUrlList = ps.DirectUrl;
                        }
                        else if (ps.ButtonLink == "00")
                        {
                            buttonUrlList = Url.Page("/Contact");
                        }
                        else
                        {
                            buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                        }
                        <!-- Blog One Single -->
                        <div class="blog-one__single">
                            <div class="blog-one__img-box">
                                <div class="blog-one__img">
                                    <img src="@ps.ImageUrl" alt="@ps.Title">
                                    <div class="blog-one__tags"><span>@ps.MiniTitle</span></div>
                                </div>

                            </div>
                            <div class="blog-one__content">

                                <h3 class="blog-one__title"><a href="@buttonUrlList">@ps.Title</a></h3>
                                <p class="blog-one__text">@ps.Description</p>
                                <a href="@buttonUrlList" class="blog-one__read-more">Read More <span class="fas fa-arrow-right"></span></a>
                            </div>
                        </div>
                    }

                </div><!-- /.blog-one__carousel -->
            </div>
        </section>

    }
    else if (item.TemplateKey == "20")
    {
        <section class="process-one @item.CustomClass">
            <div class="process-one__shape-4 float-bob-y">
                <img src="@item.ImageUrl" alt="@item.Title">
            </div>
            <div class="container">
                <div class="section-title text-center sec-title-animation animation-style1">
                    <div class="section-title__tagline-box">
                        <span class="section-title__tagline">@item.MiniTitle</span>
                        <div class="section-title__tagline-shape"></div>
                    </div>
                    <h2 class="section-title__title title-animation" style="perspective: 400px;">
                        @item.Title
                    </h2>
                </div>

            </div>
        </section>
        <section class="feature-one @item.CustomClass">
            <div class="container">
                <div class="row">
                    @foreach (var ps in item.PageSectionLists)
                    {
                        string buttonUrlList = "#";

                        if (ps.ButtonLink == "0")
                        {
                            buttonUrlList = "#";
                        }
                        else if (ps.ButtonLink == "Direct")
                        {
                            buttonUrlList = ps.DirectUrl;
                        }
                        else if (ps.ButtonLink == "00")
                        {
                            buttonUrlList = Url.Page("/Contact");
                        }
                        else
                        {
                            buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                        }
                        <!-- Feature One Single Start -->
                        <div class="col-xl-4 col-lg-4 wow fadeInLeft animated" data-wow-delay="100ms" style="visibility: visible; animation-delay: 100ms; animation-name: fadeInLeft;">
                            <div class="feature-one__single">
                                <div class="feature-one__count"></div>
                                <div class="feature-one__icon">
                                    <i class="@ps.IconText"></i>
                                </div>
                                <h3 class="feature-one__title"><a href="@buttonUrlList">@ps.Title</a></h3>
                                <p class="feature-one__text">
                                    @ps.Description
                                </p>
                                <a href="@buttonUrlList" class="feature-one__read-more">
                                    @ps.ButtonText <span class="icon-right-arrow"></span>
                                </a>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </section>
    }
    else if (item.TemplateKey == "21")
    {
        <section class="services-three @item.CustomClass">
            <div class="container">
                <!-- Section Title -->
                <div class="section-title text-center sec-title-animation animation-style1">
                    <div class="section-title__tagline-box">
                        <span class="section-title__tagline">@item.MiniTitle</span>
                        <div class="section-title__tagline-shape"></div>
                    </div>
                    <h2 class="section-title__title title-animation" style="perspective:400px;">
                        @item.Title
                    </h2>
                </div>

                <!-- Carousel -->
                <div class="services-three__carousel owl-carousel owl-theme">
                    @foreach (var ps in item.PageSectionLists)
                    {
                        string buttonUrlList = "#";

                        if (ps.ButtonLink == "0")
                        {
                            buttonUrlList = "#";
                        }
                        else if (ps.ButtonLink == "Direct")
                        {
                            buttonUrlList = ps.DirectUrl;
                        }
                        else if (ps.ButtonLink == "00")
                        {
                            buttonUrlList = Url.Page("/Contact");
                        }
                        else
                        {
                            buttonUrlList = Url.Page("/Source", new { id = ps.ButtonLink, title = regex.Replace(ps.Title, "-") });
                        }
                        <!-- Service Item -->
                        <div class="services-three__single">
                            <div class="services-three__icon"><i class="@ps.IconText"></i></div>
                            <h3 class="services-three__title">
                                <a href="@buttonUrlList">@ps.Title</a>
                            </h3>
                            <p class="services-three__text">@ps.Description</p>
                            <a href="@buttonUrlList" class="services-three__btn">
                                @ps.ButtonText <span class="fas fa-arrow-right"></span>
                            </a>
                        </div>
                    }

                </div><!-- /.carousel -->
            </div>
        </section>


    }
    else if (item.TemplateKey == "22")
    {
        <section class="process-one @item.CustomClass">

            <div class="container">
                <div class="section-title text-center sec-title-animation animation-style1">
                    <div class="section-title__tagline-box">
                        <span class="section-title__tagline">@item.MiniTitle</span>
                        <div class="section-title__tagline-shape"></div>
                    </div>
                    <h2 class="section-title__title title-animation" style="perspective: 400px;">
                        @item.Title
                    </h2>
                </div>
                <div class="section-body">
                    @Html.Raw(item.FullDescription)
                </div>
            </div>
        </section>

    }

    <div class="clearfix"></div>
}

